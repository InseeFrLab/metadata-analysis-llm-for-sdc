# Extra dependencies for the presentation notebook (notebook/demo_pipeline_for_minio.ipynb).
# That notebook runs the full pipeline live on Onyxia: it calls the model and reads/writes
# MinIO, so it also needs `openai` (from requirements.txt) and `s3fs` (pre-installed on the
# Onyxia image; otherwise `pip install s3fs`). This file only adds the Jupyter tooling and
# pandas on top.
#
# Install (on top of requirements.txt):
#   pip install -r requirements-notebook.txt

jupyter==1.1.1        # run the notebook (jupyter lab / nbconvert)
ipykernel==6.29.5     # Python kernel for the notebook
pandas==2.3.2         # pretty CSV display in the notebook (also used by the optional .rds path)
